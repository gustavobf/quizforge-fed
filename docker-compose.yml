version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quizforge-fed
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - quizforge_quizforge-network

networks:
  quizforge_quizforge-network:
    external: true